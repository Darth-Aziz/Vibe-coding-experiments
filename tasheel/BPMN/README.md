# BPMN reference

Archived **BPMN / workflow design** exports (e.g. zip bundles). Used as **reference** for the in-app workflow designer (`@xyflow/react`), not as a runtime dependency.

Prefer keeping large **`.zip`** files local or in object storage unless you adopt **Git LFS**.
